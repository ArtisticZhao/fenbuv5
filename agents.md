# 项目简介

本项目是一个带有时滞参数的反应-扩散仿真数值程序，用于研究系统在 Hopf 分岔附近的动力学行为。

程序整体分为两个阶段：

1. 利用 Hopf 分支理论寻找分支参数；
2. 对 Hopf 分岔点进行数值仿真。

后续对话或代理执行任务时，可以优先参考这一背景：项目的核心目标不是通用 PDE 求解器，而是围绕带时滞反应-扩散系统的 Hopf 分岔参数定位与分岔点附近的数值模拟展开。

## Python 环境

本项目运行 Python 程序时，默认使用 conda 环境 `note`。
